# Fulcycle - Chanllege Mysql, Node e Nginx

## Instalation

#### First it's necessary create a mysql database volume and table people

#### Then, run docker-compose 
```
docker-compose up -d --build
```
